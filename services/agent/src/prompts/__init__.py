"""The agent's prompts: the text the model is given as instructions.

System prompts and tasks for each agent, the rules, the default soul, the setup checklist, the
compaction prompts and mid-run reminders. Other text (tool results, errors, progress labels, the
CLI and the report) lives beside the code that uses it. Tool descriptions stay as docstrings on
the tools, which LangChain reads.
"""
