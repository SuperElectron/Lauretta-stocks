This file is a throwaway proof for issue #21 that the `secrets` CI job blocks
a leaked credential. The value below is FAKE and was never valid; it matches
gitleaks' generic-api-key rule locally (verified) without matching a GitHub
push-protection vendor pattern, so it reaches gitleaks in CI instead of being
blocked at push time.

api_key = "AbC123XyZ789QwErTyUiOpAsDfGhJk"
